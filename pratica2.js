let n = parseInt(prompt('Quantos números? '))
const numeros = []
for (let i = 0; i < n; i++) {
  numeros[i] = parseFloat(prompt(`Número ${i + 1}: `));
}
for (let i = n - 1; i >= 0; i--){
}
document.writeln("Vetor na ordem inversa:"+ n[i]);